# Базовый префикс API. По умолчанию /api (через nginx).
# Для локального запуска без nginx: http://localhost:8000/api
VITE_API_BASE_URL=/api

# Sitekey Яндекс SmartCaptcha (клиентский). Если пусто — капча отключена (dev).
VITE_SMARTCAPTCHA_SITEKEY=
